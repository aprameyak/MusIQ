. "$(dirname "$0")/_/husky.sh"

npx lint-staged

echo "Checking types in backend..."
cd backend && npx tsc --noEmit && cd ..

echo "Checking types in webapp..."
cd webapp && npx tsc --noEmit && cd ..

echo "Checking types in etl..."
cd etl && npx tsc --noEmit && cd ..
